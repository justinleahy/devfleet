{
  "name": "command-center-pi-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Pi orchestration worker process for the Pi Command Center runtime. Speaks the versioned NDJSON stdio protocol (protocolVersion 1) defined in SPEC.md section 24.",
  "type": "module",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"pi-worker/tests/*.test.ts\""
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.0",
    "@earendil-works/pi-server": "0.85.0",
    "typebox": "^1.3.7"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "typescript": "5.9.3"
  }
}
